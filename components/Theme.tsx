import React from "react";
import { FaMoon, FaSun } from "react-icons/fa";

const Theme = () => {
	return (
		<section>
			<button>
				<FaSun />
			</button>
			<button>
				<FaMoon />
			</button>
		</section>
	);
};

export default Theme;
